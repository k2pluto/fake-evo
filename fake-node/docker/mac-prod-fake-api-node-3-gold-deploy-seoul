#! /bin/bash
BASEDIR=$(dirname $0)
cd $BASEDIR

clusterId=fake-api-gold
serviceName=fake-api-gold-srv
regionName=ap-northeast-2

aws ecs update-service --cluster $clusterId --service $serviceName --region $regionName --force-new-deployment --profile faker