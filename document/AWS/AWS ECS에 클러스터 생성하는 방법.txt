1. AWS/ECS 클러스터 탭으로 이동

2. 클러스터 생성 버튼 클릭

3. EC2 Linux + 네트워킹 선택

4.

클러스터 이름 입력

온디멘드 인스턴스 선택

EC2 인스턴스 유형 및 갯수 선택

EC2 AMI ID 는 그대로 둠

EBS 스토리지도 왠만하면 그대로

키 페어 선택

VPC는 새 VPC 생성이 아니라 기존의 것을 사용한다.

서브넷은 전체 선택

보안그룹을 미리 만든 후 새로고침해서 선택

보안 그룹 인바운드 규칙은 그대로 둠

컨테이너 인스턴스 IAM 역할도 그대로 둠

CloudWatch 컨테이너 인사이트는 disable 상태로 둠

5. 생성